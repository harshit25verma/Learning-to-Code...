#include <cs50.h>
#include <stdio.h>
#include <ctype.h>
#include <math.h>

int main(void)
{
    string text = get_string("Text: ");
    
    printf("Number \n");
}

int count_letters(string text)
{
    count_letters = string_length(text);
    return text;
}