#include <cs50.h>
#include <stdio.h>

int main(void)
{
  int h = get_int("Height: ");
  int s = h - 1;
  int n = 1;
  if(h <= 8 && h >= 1)
  {
    for()
    {
    printf("#");
    }
    if(h <= 8 && h >= 1)
    {
    printf("  ");
    }
    if(h <= 8 && h >= 1)
    {
     printf("#\n");
    }
    s--;
    n++;
  }
}