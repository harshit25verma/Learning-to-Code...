#include <cs50.h>
#include <stdio.h>

int main(void)
{
    for()
    {
    if(height <= 8 && height >= 1)
    {
        printf("##\n");
    }
    else
    {
        while(height > 8 || height < 1)
        {
        get_int("Height: ");
        }
    }
}

