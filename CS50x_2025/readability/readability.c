#include <stdio.h>
#include <cs50.h>
#include <ctype.h>

int NumberOfWords();
int NumberOfLetters();
int NumberOfSentences();

int main (void)
{
    while (word[n] = '\0')
    {
    string word = get_string("Text: ")     //get prompt from the User
    }

    //Check how many word and letter



        //Check how many Letters are present in the first 100 words
        //Count words until 100 words
        if(word_count = 100)
        {

        }
        else
        {
            break;
        }
        //Check How many Sentences are present in the first 100 words

    //Calculate via the Coleman-Liau index

    //Print the answer

}


//Check how many word
int NumberOfWords()
{
    int word_count = 0;
    while(isspace(word[n]))
    {
        word_count++;
    }
}

//Check how many letters
int NumberOfLetters()
{
    int letter_count = 0;
    while(isalnum(word[n]))
    {
        letter_count++;
    }
}

//Check how many sentences
int NumberOfSentences()
{
    int sentence_count = 0;
    while(ispunct(word[n]))
    {
        sentence_count++;
    }
}
