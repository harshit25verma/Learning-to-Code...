#include <stdio.h>
#include <cs50.h>

int main(void)
{
    do
    {
        int height = get_int("Height: ");
    }
    while(height<1);

    int rows;
    for (int rows = 0; rows<height; rows++)
    {
        
    }
}
